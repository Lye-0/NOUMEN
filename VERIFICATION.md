# 検証記録

検証日: 2026-09-10

## 実施環境

- Chromium 144.0.7559.96 / Linux
- Playwright の Python API によるブラウザ操作
- WebGL は ANGLE / SwiftShader のソフトウェア描画
- Node.js v22.16.0

実機iPhoneや実機GPUでの検証ではありません。フレームレートの数値保証は行っていません。

## 結果

### データ・数学・構成: 12 / 12 PASS

`node --test` で実行しました。

1. 五つの天体のID・日本語解説・データが揃っていること
2. 天体ごとに異なるカメラ設定と左右の配置があること
3. ベクトル演算とゼロベクトルの処理
4. カメラ座標系の正規直交性
5. 投影位置の方向と中心
6. 天体選択用の光線と球の交差判定
7. カメラ補間の端点・単調性
8. HTML / CSS / JavaScript / 画像の分離と相対参照
9. JavaScript が参照する操作要素の存在
10. 3Dの球・破断外殻・立体環の交差処理
11. 六つの展示のPC用・スマートフォン用フォールバック画像
12. GLSLのsmoothstepが定義された順序の数値端点を使っていること

### 実際の3D描画: 12画面を確認

1440 × 960 と 390 × 844 の各サイズで、俯瞰と五つの個別展示を描画しました。WebGLが成功し、静止画へのフォールバックが発動していないことを確認しています。シェーダーのコンパイルエラー、未捕捉のJavaScript例外はありませんでした。

画面構成を確認し、同じレンダラーの描画結果を `assets/fallback-*.jpg` に保存しています。これらは未実装のイメージ図ではなく、3D展示から出力した画像です。

### 実ブラウザでの操作: 26 / 26 PASS

390 × 667 を中心に、実際のWebGLを動かした状態で検証しました。

- WebGL初期化、スマートフォンの初期品質「標準」、動きを減らす設定での初期停止
- タッチでの天体ラベル選択と、五つの展示ナビゲーション
- 解説の末尾までのスクロール、末尾でのスクロール案内の非表示
- 本館についてのダイアログ、Escによるダイアログだけの終了
- キーボード左右移動と末尾・先頭の循環
- 前の天体 / 次の天体のボタン
- Esc / 戻るリンクからの全体展示への復帰
- 描画品質の高 / 低 / 標準への切り替え
- 再開によるシーン時間の進行、一時停止中のシーン時間の固定
- 844 × 390 への横画面変更と、ページ全体の横方向・縦方向の収まり
- 未捕捉のJavaScript例外がないこと

### WebGLがない場合: 12 / 12 PASS

テスト環境でWebGLコンテキストの取得を失敗させ、PC / スマートフォンの各六つの展示に切り替えました。すべての静止画が読み込まれ、展示の選択と解説が使用できました。3D専用の品質・停止操作が無効になることと、ローディング画面が残らないことも確認しました。

### 分割ファイルのHTTP配信

同梱の `tools/serve.cjs` から各ファイルを取得し、配信された内容が元ファイルとバイト単位で一致することを確認しました。HTML、CSS、JS、JPEG、SVGのContent-Typeも確認しました。

別のローカルHTTPサーバーでは `/noumen/` というサブディレクトリから各ファイルを取得し、元ファイルとの一致を確認しました。

## 検証上の制約

この実行環境では、管理されたブラウザからローカルHTTP URLへの直接移動が制限されています。そのため、ブラウザの描画・操作検証には、分割ソースと画像をそのまま埋め込んだプレビューHTMLを使用しました。分割構成のHTTP配信は、上記のとおり別に検証しています。

次の項目は未実施です。

- 実際のGitHub Pages URLへの公開と、公開後のブラウザでの確認
- 実機iPhone / Safari、Firefox、Android各端末での確認
- 実機GPUのフレームレート・消費電力・長時間使用時の発熱の測定
- WebGLコンテキスト消失後の自動復元処理の実機検証

高負荷の端末では描画品質「低」を使用してください。コンテキスト消失時には静止画を表示して復元を試みる処理を実装していますが、その復元をすべての端末で保証するものではありません。
