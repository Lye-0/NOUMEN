# 検証記録 — NOUMEN 1.2.0

検証日：2026-09-10

## 検証環境

Chromium 144.0.7559.96（Linux）、PlaywrightのPython API、ANGLE / SwiftShaderによるWebGL描画、Node.js v22.16.0を使用しました。

実機iPhoneや実機GPUでの計測ではありません。以下の結果は、この環境で実行した確認の記録です。

## Node.jsテスト：37 / 37 PASS

プロジェクト直下の `node --test` で実行しました。追加のnpmパッケージは不要です。

### 展示データ・数学・参照ファイル

5天体のIDと日本語解説、独立したカメラ構図、ベクトル演算、カメラ座標系、投影、光線と球の選択判定、補間、HTMLが参照するファイル、操作要素、PC・縦画面の代替静止画を検証しました。

### 改良描画の整合性

VIGILの形状の有限性、頂点法線、惑星との離隔、材質区分、再生成の決定性、LACUNAの球面細分化と連続ノイズを検証しました。構造物の独立した描画・影パス、符号化深度に対するNEARESTサンプリング、共通の光源投影、主要シェーダーの8テクスチャユニット以内の使用、依存コードの展開、環の透過と扁平球、スクリプトの読込順も確認しています。

LACUNAとGEMINAの6つの既存素材ファイルは、前回の改良版とSHA-256が一致しています。コンテキスト消失時に無効なGPUハンドルを破棄し、復元後の新しいコンテキストへ持ち越さない処理もテストしました。

### 任意のローカルHTTPサーバー

HTML・全CSS/JS・素材の配信とContent-Type、存在しないファイルの404、変更を伴うHTTPメソッドの拒否、隠しパス・エンコードされた上位パスへのアクセス拒否、HEADの動作を検証しました。

## 実際のWebGL描画：12画面

**1600×1000**と**390×844**で、それぞれ俯瞰・5天体を描画し、実際の画面を保存しました。各画面でWebGLが有効であり、静止画へのフォールバックが発生していないことを確認しています。未捕捉のJavaScript例外、シェーダーのコンパイル失敗はありませんでした。

`assets/fallback-*.jpg` の12枚は、この版のWebGLキャンバスを保存したものです。説明文やUIを焼き込まず、静止画モードでも通常の展示UIを重ねる構成です。VESPERのスマートフォン用画像は、縁が見えるように修正した専用カメラで出力しています。

検証時の描画情報：5シェーダープログラム、LACUNA 108,508三角形、VIGIL 135,568三角形・追加部材1,428個。PCの標準設定における構造物の影は2048×2048です。

## 実ブラウザでの操作・復元・静止画確認：51 / 51 PASS

PCの**1280×900**、縦画面の**390×844 / DPR 2 / タッチ入力エミュレーション**を使用しました。

確認項目は、WebGL初期化、動きを減らす設定での初期停止、スマートフォンの初期品質「標準」、5天体の選択、前後ボタンの循環、キーボード左右移動、Escによる復帰、ダイアログの開閉、俯瞰の天体ラベル、低・標準・高の切替、再生による時間の進行、一時停止中の時間固定です。

通常速度のカメラ移動が始まって終了すること、短時間に展示を複数回変更した場合に最後の選択へ到達することも確認しました。縦画面では各天体をタッチで選び、解説の末尾までスクロールできることと、ページ全体に横方向のはみ出しがないことを確認しています。844×390への横画面変更も確認しました。

`WEBGL_lose_context` 拡張で実際に描画コンテキストを失わせ、静止画への切替と、GPU資源を作り直した後の3D復元を検証しています。これはソフトウェア描画環境での結果であり、実機全般での復元を保証するものではありません。

操作・復元・縦画面・静止画を合わせて51項目がすべて通過しました。最終実行では未捕捉のJavaScript例外とWebGL警告はありませんでした。

## WebGL非対応時の確認

PC幅・縦画面の両方でコンテキスト取得を意図的に失敗させ、俯瞰と全5天体の静止画が読み込めることを確認しました。展示の選択と日本語解説は使用でき、品質・停止の3D専用操作は無効になります。ローディング画面が残らないことも確認しました。

## 単独プレビューと公開用分割版

単独プレビューHTMLは、`tools/make-preview.cjs` で公開用プロジェクトの実ファイルから生成しています。HTML・CSS・9本のJavaScript・全画像を内包し、公開用分割版の描画ロジックを別実装で置き換えることはしていません。

配布用単独HTMLそのものをブラウザの文書へ読み込む確認も実施しました。俯瞰のWebGL起動とVIGILへの移動が成功し、外部HTTPリクエスト、JavaScript例外、WebGL警告はありませんでした。単独版に埋め込んだ21画像・9本のJavaScript・CSSが分割版と一致することも確認しています。

## 検証上の制約

この環境では、管理されたブラウザからローカルHTTP URLへの直接移動が制限されています。ブラウザでの描画・操作は、**実際の分割ソースと画像をabout:blankの文書へ読み込んで**検証しました。HTTPサーバーからの分割ファイル配信はNode.jsのテストで別に確認しています。管理ポリシーの変更は行っていません。

実際のGitHub Pages URLへのデプロイと公開後の表示、実機Safari・Firefox・Android各端末、実機GPUのフレームレート・消費電力・長時間の発熱は未検証です。

高解像度素材と複数の描画パスを使用するため、端末によって負荷が異なります。速度が不足する場合は描画品質「低」または一時停止を使用してください。
